# CopyCode

A responsive, dark-themed code snippet viewer with one-click clipboard copying.

## Features
- Independent code cards for React Router components
- One-click copy with Clipboard API and fallback support
- Synchronized line numbers and smooth scrolling
- Clean split architecture (`index.html`, `style.css`, `script.js`)
